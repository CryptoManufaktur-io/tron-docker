services:
  tron:
    ports:
      - ${HTTP_FULL_NODE_PORT:-8090}:${HTTP_FULL_NODE_PORT:-8090}
      - ${HTTP_SOLIDITY_NODE_PORT:-8091}:${HTTP_SOLIDITY_NODE_PORT:-8091}
      - ${JSONRPC_HTTP_FULL_NODE_PORT:-8545}:${JSONRPC_HTTP_FULL_NODE_PORT:-8545}
      - ${JSONRPC_HTTP_SOLIDITY_PORT:-8555}:${JSONRPC_HTTP_SOLIDITY_PORT:-8555}
